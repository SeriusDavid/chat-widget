import 'package:flutter_test/flutter_test.dart';
/* import 'package:chat_widget/chat_widget.dart';
   import 'package:web_socket_channel/io.dart';
   import 'package:flutter/material.dart'; */
   
void main() {
  test('adds more messages to chat', () {
    /*
    IOWebSocketChannel channel;
    List<Widget> messages = [
      ListTile(
        title: Text("data title"),
        subtitle: Text("data content"),
      ),
      ListTile(
        title: Text("data title 1"),
        subtitle: Text("data content 1"),
      )
    ];

    onSubmit(text, chat){
      //setState(() {});
      chat.messages.insert(0, 
        ListTile(
          title: Text("data title insert"),
          subtitle: Text("data content insert"),
        )
      );
    }

    onData(){

    }

    */
  });
}
